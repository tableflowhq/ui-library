import React from "react";
import { DialogProps } from "./types";
export default function Dialog({ icon, variants, iconPosition, items, dialogPosition, useActiveAsLabel, ...props }: DialogProps): React.ReactElement;
