/// <reference types="react" />
import { PaginationProps } from "./types";
export default function Pagination({ totalItems, itemsPerPage, initialPage, paginatorSize, onPageChange, showNumbers, showArrows, showFirstLast, }: PaginationProps): React.ReactElement;
