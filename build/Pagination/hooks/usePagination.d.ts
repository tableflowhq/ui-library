import { PaginatorType } from "../types";
declare const usePaginator: (totalItems: number, itemsPerPage: number, initialPage: number, paginatorSize?: number) => PaginatorType;
export default usePaginator;
